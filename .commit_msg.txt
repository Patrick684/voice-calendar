fix: storage 软删除过滤遗漏 + CI 增加 format 检查 + pre-commit hook

## 功能描述
- get_event_by_id 添加 deleted_at IS NULL 过滤（软删除后不再返回已删除事件）
- get_event_count 添加 deleted_at IS NULL 过滤（计数排除已删除事件）
- CI workflow 增加 ruff format --check
- 新增 .qoder/hooks/pre-commit（ruff check + format + 全量测试）

## 实现思路
修复 verify_calendar.py 测试失败：delete_event 改为软删除后，
get_event_by_id 和 get_event_count 未同步添加 deleted_at 过滤条件

## 测试方法
.venv\Scripts\python.exe tests/verify_calendar.py 通过
.venv\Scripts\python.exe tests/verify_command.py 通过
